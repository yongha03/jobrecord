server:
  port: 8080
  tomcat:
    max-swallow-size: -1

spring:
  # 이메일 발송(SMTP) 설정
  mail:
    host: smtp.gmail.com
    port: 587
    username: jobrecord.noreply@gmail.com
    password: fver ypvg ceen teem
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  # Redis 설정 추가
  data:
    redis:
      host: 127.0.0.1
      port: 6379

  web:
    resources:
      add-mappings: true   # 정적 리소스 매핑 활성 (안심용)
  thymeleaf:
    cache: false           # 개발 중 템플릿 캐시 끔

  datasource:
    url: jdbc:mysql://127.0.0.1:3307/jobproj_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=utf8&allowPublicKeyRetrieval=true&allowMultiQueries=true
    username: db_user
    password: db_password
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connectionInitSql: "SET NAMES utf8mb4"

  jackson:
    serialization:
      write-dates-as-timestamps: false

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      resolve-lazily: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

management:
  endpoints:
    web:
      base-path: /api/actuator
      exposure:
        include: health,info
  health:
    db:
      enabled: false

jwt:
  secret-key: "Sm9iUmVjb3JkX1Byb2plY3RfU2VjcmV0X0tleV9FbmNvZGVfUGxlYXNl"
  expiration-ms: 3600000
  refresh-expiration-ms: 1209600000

springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  cache:
    disabled: true
  swagger-ui:
    path: /docs
    use-root-path: false
