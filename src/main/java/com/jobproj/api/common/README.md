# common

> 공용 유틸/응답/페이지네이션 등 **도메인 무관** 기능.

## 포함(예시)
- `ApiResponse<T>`: 성공/실패 표준 응답 래퍼
- `PageRequest`, `PageResponse`: 페이지네이션 입력/출력 DTO
- (필요 시) Date/Number 포맷 유틸

## 규칙
- 비즈니스 로직/보안/DB 접근 코드는 금지
- 공통 타입은 **의존 방향의 끝단**이 되도록(상위 계층에서 재사용)
